--- src/prop_ext.rs	2006-07-24 04:21:28
+++ src/prop_ext.rs	2026-07-09 11:01:22
@@ -219,12 +219,10 @@
     /// Get the patched highlight style with focused or unfocused style.
     #[inline]
     pub fn get_style_focus(&self, normal_style: Style, focus: bool) -> Style {
-        let style = normal_style.patch(self.style);
-
         if focus {
-            style
+            normal_style.patch(self.style)
         } else {
-            style.patch(self.style_unfocused)
+            self.style_unfocused
         }
     }
 }
--- src/components/table.rs	2006-07-24 04:21:28
+++ src/components/table.rs	2026-07-09 11:08:06
@@ -350,7 +350,9 @@
                     .get_style_focus(self.common.style, self.common.is_active()),
             );
 
-        if let Some(block) = self.common.get_block() {
+        let block = self.common.get_block();
+        let content_area = block.as_ref().map_or(area, |b| b.inner(area));
+        if let Some(block) = block {
             widget = widget.block(block);
         }
 
@@ -379,7 +381,8 @@
                     .collect()
             })
             .unwrap_or_default();
-        if !headers.is_empty() {
+        let has_header = !headers.is_empty();
+        if has_header {
             widget = widget.header(
                 Row::new(headers)
                     .style(self.common.style)
@@ -390,6 +393,26 @@
             let mut state: TableState = TableState::default();
             state.select(Some(self.states.list_index));
             render.render_stateful_widget(widget, area, &mut state);
+
+            if self.common.is_active() {
+                if let Some(fill_bg) = self.common_hg.style.bg {
+                    if let Some(selected) = state.selected() {
+                        let header_count = if has_header { 1 } else { 0 };
+                        let offset = state.offset();
+                        let visual_row = selected.saturating_sub(offset);
+                        let row_y = content_area.y + header_count as u16 + visual_row as u16 * row_height;
+                        if row_y < content_area.bottom() {
+                            let buf = render.buffer_mut();
+                            let left_edge = content_area.x;
+                            let right_edge = content_area.right().saturating_sub(1);
+                            if right_edge > left_edge {
+                                buf[(left_edge, row_y)].set_symbol("").set_fg(fill_bg).set_bg(Color::Reset);
+                                buf[(right_edge, row_y)].set_symbol("").set_fg(fill_bg).set_bg(Color::Reset);
+                            }
+                        }
+                    }
+                }
+            }
         } else {
             render.render_widget(widget, area);
         }
--- src/components/list.rs	2006-07-24 04:21:28
+++ src/components/list.rs	2026-07-09 11:08:11
@@ -267,7 +267,9 @@
                     .get_style_focus(self.common.style, self.common.is_active()),
             );
 
-        if let Some(block) = self.common.get_block() {
+        let block = self.common.get_block();
+        let content_area = block.as_ref().map_or(area, |b| b.inner(area));
+        if let Some(block) = block {
             widget = widget.block(block);
         }
 
@@ -280,6 +282,25 @@
             let mut state: ListState = ListState::default();
             state.select(Some(self.states.list_index));
             render.render_stateful_widget(widget, area, &mut state);
+
+            if self.common.is_active() {
+                if let Some(fill_bg) = self.common_hg.style.bg {
+                    if let Some(selected) = state.selected() {
+                        let offset = state.offset();
+                        let visual_row = selected.saturating_sub(offset);
+                        let row_y = content_area.y + visual_row as u16;
+                        if row_y < content_area.bottom() {
+                            let buf = render.buffer_mut();
+                            let left_edge = content_area.x;
+                            let right_edge = content_area.right().saturating_sub(1);
+                            if right_edge > left_edge {
+                                buf[(left_edge, row_y)].set_symbol("").set_fg(fill_bg).set_bg(Color::Reset);
+                                buf[(right_edge, row_y)].set_symbol("").set_fg(fill_bg).set_bg(Color::Reset);
+                            }
+                        }
+                    }
+                }
+            }
         } else {
             render.render_widget(widget, area);
         }
